# Use /api in development (Vite proxies to backend — required for HTTP-only cookies)
VITE_API_URL=/api

# Backend origin for Google OAuth redirect (must match Google Cloud redirect URI host)
VITE_API_ORIGIN=http://localhost:5000

# Same client ID as backend GOOGLE_CLIENT_ID (public — safe in frontend)
VITE_GOOGLE_CLIENT_ID=
